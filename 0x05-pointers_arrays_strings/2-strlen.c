#include "main.h"
/**
* _strlen - a functiomn that gives the length of tring
*@s: length of the string.
* Return: always 0.
*/
int _strlen(char *s)
{
int i = 0;
while (s[i] != 0)
{
i++;
}
return (0);
}
