#include "main.h"
/**
 * _puts - a function that prints a string
 * @str:
 * Return: always 0.
 */
void _puts(char *str)
{
char *str;
str = "I do not fear computers. I fear the lackof thm - Isaac Asi,ov";
puts(str);
return (0);
}
